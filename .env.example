# Client-side defaults
VITE_KIMI_BASE_URL=/api/kimi
VITE_KIMI_MODEL=moonshotai/kimi-k2.5

# Server-side reverse proxy target (Cloudflare Pages Function / local Vite proxy)
KIMI_UPSTREAM=https://integrate.api.nvidia.com

# Optional server-side secret for proxy mode (Cloudflare Pages Secret)
# KIMI_API_KEY=
